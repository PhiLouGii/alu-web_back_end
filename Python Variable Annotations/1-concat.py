#!/usr/bin/env python3
"""Concatenate two strings."""


def concat(str1: str, str2: str) -> str:
    """
    A function that takes two arguments, `str1` and `str2`,
    which are expected to be strings,
    and returns the concatenation of these two strings.
    """
    return str1 + str2